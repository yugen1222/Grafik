# Safia Schedule v15 Core Stable

Исправлен критический запуск приложения: ранние render() и KPI обращались к неделе до загрузки Firebase, из-за чего JavaScript останавливался и все разделы оставались пустыми.

- интерфейс ждёт загрузку Firebase;
- сотрудники, недели, штатка и история отображаются после получения state;
- ранняя инициализация KPI безопасна;
- Firebase и существующие данные не очищаются.
